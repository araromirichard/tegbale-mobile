import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tegbale.mobile',
  appName: 'tegbale-mobile',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
